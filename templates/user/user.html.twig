{% extends 'base.html.twig' %}

{% block body %}
    <body>
    <!-- END nav -->

    <section class="hero-wrap hero-wrap-2 js-fullheight" style="background-image: url('images/bg_3.jpg');" data-stellar-background-ratio="0.5">
        <div class="overlay"></div>
        <div class="container">
            <div class="row no-gutters slider-text js-fullheight align-items-end justify-content-start">
                <div class="col-md-9 ftco-animate pb-5">
                    <p class="breadcrumbs"><span class="mr-2"><a href="{{ path('app_homepage') }}">Home <i class="ion-ios-arrow-forward"></i></a></span> <span>Utilisateur <i class="ion-ios-arrow-forward"></i></span></p>
                    <h1 class="mb-3 bread">Mon compte</h1>
                </div>
            </div>
        </div>
    </section>


    <div class="container">
        <h1>Mon Compte</h1>

        <div class="card">
            <div class="card-body">
                <p class="card-text">Nom : {{ utilisateur.fullname }}</p>
                <p class="card-text">Email : {{ utilisateur.email }}</p>
                <p class="card-text">Adresse : {{ utilisateur.adress }}</p>
                <p class="card-text">Code postal : {{ utilisateur.zipcode }}</p>
                <p class="card-text">Ville : {{ utilisateur.city }}</p>
            </div>
        </div>

        <a href="{{ path('app_logout') }}" class="btn btn-primary mt-3">Se déconnecter</a>
    </div>
    </body>
{% endblock %}
